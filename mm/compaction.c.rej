--- mm/compaction.c
+++ mm/compaction.c
@@ -372,7 +372,8 @@
 		}
 
 		if (!cc->sync)
-				mode |= ISOLATE_CLEAN;
+			mode |= ISOLATE_ASYNC_MIGRATE;
+
 		/* Try isolate the page */
 		if (__isolate_lru_page(page, mode, 0) != 0)
 			continue;
